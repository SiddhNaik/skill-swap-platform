// SwapForm.jsx - Form for creating swap requests

import React from 'react';

const SwapForm = () => {
  return (
    <form className="space-y-4">
      {/* TODO: Add form fields for swap request */}
      <button type="submit" className="bg-blue-500 text-white px-4 py-2 rounded">Request Swap</button>
    </form>
  );
};

export default SwapForm; 